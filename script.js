const header=document.querySelector('.site-header');const menu=document.querySelector('.menu-button');const nav=document.querySelector('.nav-links');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const animated=[...document.querySelectorAll('.reveal,.stagger>*')];addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>50);if(!reduce)animated.forEach(el=>{const box=el.getBoundingClientRect();el.classList.toggle('passed',box.bottom<80)})},{passive:true});menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));if(reduce){animated.forEach(el=>el.classList.add('in-view'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in-view');entry.target.classList.remove('passed')}}),{threshold:.12});animated.forEach(el=>observer.observe(el))}
